{
  "_comment": "TEMPLATE ONLY. Copy to Secrets.json (gitignored) for local dev, or set these as environment variables (ConnectionStrings__CommBank, Jwt__SigningKey). Never place real secrets in this committed file.",
  "ConnectionStrings": {
    "CommBank": "mongodb+srv://<USERNAME>:<PASSWORD>@<CLUSTER-HOST>/<DATABASE>?retryWrites=true&w=majority"
  },
  "Jwt": {
    "SigningKey": "<64+ random characters; HS256 needs >= 32 bytes. Generate with: openssl rand -base64 48>"
  }
}
